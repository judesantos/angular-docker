version: '3.5'

services:

    angular-app-prod:
        container_name: angular-app-prod
        build:
            context: .
            dockerfile: Dockerfile-prod
        ports:
            - '80:80'
